{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Sass Compile",
      "type": "shell",
      "command": "sass",
      "args": ["${file}", "${fileDirname}/${fileBasenameNoExtension}.css"],
      "group": "build",
      "problemMatcher": ["$node-sass"]
    }
  ]
}
